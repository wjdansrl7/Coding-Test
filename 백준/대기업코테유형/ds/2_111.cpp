#include <iostream>
#include <vector>
using namespace std;

int main(int argc, char const *argv[])
{
    int T;
    cin >> T;

    for (int tc = 1; tc <= T; tc++)
    {
        int n, m, q;
        cin >> n >> m >> q;

        vector<vector<int>> v(n + 1, vector<int>(m + 1, 0));
    }

    return 0;
}
