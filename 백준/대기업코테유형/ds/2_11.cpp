#include <iostream>
#include <vector>
using namespace std;

int main(int argc, char const *argv[])
{
    int T;
    cin >> T;

    for (int tc = 1; tc <= T; tc++)
    {
        int n, m, q;
        cin >> n >> m >> q;

        vector<int> maxCnt(m + 1, 0);
        vector<int> maxIdx(m + 1, 0);

        for (int i = 0; i < n; i++)
        {
            
        }
        
    }

    return 0;
}
