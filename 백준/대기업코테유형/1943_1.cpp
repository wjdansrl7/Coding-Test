#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    for (int tc = 0; tc < 3; tc++)
    {
        int n, money, cnt;
        cin >> n;
        int dp[50001] = {0,};

        for (int i = 0; i < n; i++)
        {
            cin >> money >> cnt;

        }
        

    }
    
    return 0;
}
