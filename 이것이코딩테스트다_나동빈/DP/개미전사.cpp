// i번째 식량에 대하여, (i-1)번째 식량을 약탈할 경우, i번째 식량은 약탈할 수 없다.
// (i-2)번째 식량을 약탈할 경우, i번째 식량을 약탈할 수 있다. (i-3)번째는 고려할 필요가 없는게
// (i-1)과 (i-2)번째의 해를 구하는 과정에서 이미 계산이 되었기 때문이다.
#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{

    return 0;
}
